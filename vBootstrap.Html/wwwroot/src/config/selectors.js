(function (global) {
    "use strict";
    namespace('vBootstrap.config').selectors = {
        editor: '.v-bootstrap-editor',
        bootstrap: {
            container: '.container',
            row: '.row',
            column: '[class*="col-"]',
            button: '.btn'
        }
    };
})(window);